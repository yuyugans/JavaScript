var arr = ['tes', 'nyoba', 12]
var posArr = arr.indexOf('tes')
var valArr = arr.includes('tes')


if (valArr){
    document.writeln('itu terdapat di index ke-'+posArr)
}else{
    document.writeln("gada anjing")
}